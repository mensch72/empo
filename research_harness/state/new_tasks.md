<!--
SCHEMA: new_tasks.md  (follow-up inbox)

The iteration agent appends follow-up tasks here (same block format as
task_queue.md). After each claude invocation the driver DRAINS this file:
every "## task:" block is moved into task_queue.md and this file is reset to
just this header. Keep blocks in the exact task_queue.md format, including a
REQUIRED `verification:` line, or the driver will reject them.
-->
