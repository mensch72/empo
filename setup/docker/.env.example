# Environment variables for Docker Compose
# Copy this file to .env and customize as needed
# Note: .env is gitignored to protect sensitive data

# Enable Docker BuildKit (required for pip cache mounts)
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# User ID mapping (for file permissions)
# Set these to match your host user to avoid permission issues
# On Linux/Mac, run: echo "USER_ID=$(id -u)" >> .env && echo "GROUP_ID=$(id -g)" >> .env
USER_ID=1000
GROUP_ID=1000

# Docker Hub Configuration (for cluster deployment)
# Required for 'make up-gpu-docker-hub' to push GPU images
DOCKER_REGISTRY=docker.io
DOCKER_USERNAME=youruser
GPU_IMAGE_TAG=gpu-latest
SIF_FILE=empo-gpu.sif

# Weights & Biases API Key
# Get from: https://wandb.ai/settings
WANDB_API_KEY=

# TensorBoard Log Directory
TENSORBOARD_LOG_DIR=./outputs

# Python Path (usually not needed to change)
PYTHONPATH=/workspace
